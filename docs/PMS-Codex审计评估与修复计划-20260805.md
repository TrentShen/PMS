# Codex 审计发现的核实评估与修复计划(2026-08-05)

> 来源:Codex 审计报告(P0 规范不一致 4 项、P1 运维稳定性 5 项、P2 性能 3 项、P3 产品 UX)。
> 方法:逐项对照当前代码核实(2026-08-05 工作区,含前端自检修复后的最新状态)。

---

## 一、逐项核实结论

### P0 规范与实现不一致(4 项全部属实,但定性不同)

| # | Codex 发现 | 核实 | 真实性与定性 |
|---|---|---|---|
| 1 | agents.md 要求 aiomysql + AsyncSession,实际是 pymysql + 同步 Session | ✅ 属实(cycles.py 16 个路由 0 个 async def,session.py 同步 engine) | **真实,文档漂移**。7-31 已记录此差异。风险是误导后续开发按 async 写,不是运行时 bug |
| 2 | agents.md 写 react-router-dom ^7.1,package.json 是 ^6.27.0 | ✅ 属实 | **真实,文档写错**。代码是 v6,文档写 v7 |
| 3 | pyproject requires-python 允许 >=3.10,<3.14,与 CI/本地 3.12 不一致 | ✅ 属实 | **真实**。本地 .venv 和 CI 都是 3.12,放宽到 3.10/3.13 可能引入版本差异 |
| 4 | fastapi 等版本与文档基本匹配 | ✅ 非问题 | Codex 自己也说"问题不大",跳过 |

### P1 生产运维与稳定性(4 项属实,1 项部分误报)

| # | Codex 发现 | 核实 | 真实性与定性 |
|---|---|---|---|
| 5 | /health 只返回 {"status":"ok"},不查 MySQL/Redis | ✅ 属实(health.py 全文就 3 行) | **真实**。风险:容器起来但 DB 连不上时,nginx 仍把流量打过去,表现为全站 500 而不是快速摘除 |
| 6 | MySQL 3307、Redis 6379 映射到宿主机 | ✅ 属实(compose.prod :43-44、:61-62) | **真实**,7-22 自检已标记过("端口映射未收回"),一直在待办里。灰度期内网环境风险可控,正式前必收 |
| 7 | init_db.py 直接 os.getenv + create_all + 硬编码 alembic 版本 | ✅ 属实,且**比 Codex 说的更糟**:① 全项目无任何引用(deploy.sh/compose/docs 都不调它);② 硬编码版本 00a82ba91945 在 alembic/versions 里根本不存在(初始迁移是 a088a1294289)——执行它会导致后续迁移错乱 | **真实,孤儿危险脚本,应直接删除** |
| 8 | fix_closed_pending.sql 和 import_objectives.py 留在根目录 | ⚠️ 部分误报:fix_closed_pending.sql **不存在**(可能是指已删除的 fix_participants.sql);import_objectives.py 存在(6-10 一次性导入脚本,13KB) | **一半真实**。现在目标导入已有正规 API,该脚本应归档或删除 |
| 9 | 前端无自动化测试 | ✅ 属实(CI 只有 lint/build/tsc) | **真实**。本轮刚改了 401 跳转/RequireRole 等核心逻辑,正是需要防回归的时候 |

### P2 性能与可扩展性(属实但建议维持现状)

| # | Codex 发现 | 评估 |
|---|---|---|
| 10 | 同步 SQLAlchemy 阻塞 uvicorn 工作线程 | 属实,但 pool_size=20 + pool_pre_ping 已配置;企微 H5 百人低并发无感。**不修**,用户数 200+ 再评估迁移 AsyncSession |
| 11 | 导出一次性组装进内存有 OOM 风险 | 属实,但导出限 200 行(项目约定),BytesIO 驻留几十 KB,实际风险极低。**不修**;若未来放开行数上限,届时改流式 |
| 12 | antd chunk 1.17MB | 8-05 自检已定为"有意不修"(antd 5 无可靠瘦身途径)。维持 |

### P3 产品/UX

Codex 自己也注明"已大部分修复"。其建议的剩余项(StatusTag 统一、403 返回首页、英文残留)**已在 8-05 自检修复中全部完成**,无需行动。

---

## 二、修复计划

### 批次 1:立即执行(零风险,约 2 小时)

| 项 | 动作 | 说明 |
|---|---|---|
| 文档统一(#1/#2) | 改 AGENTS.md:数据库栈改为"pymysql + 同步 Session(项目实际,勿按 async 写)";react-router-dom 改为 ^6.27;同步检查 HARNESS.md 是否有同样错误 | 消除对后续开发/AI 的误导 |
| Python 版本(#3) | pyproject requires-python 收紧为 `>=3.12,<3.13` | 与 CI/.venv 一致 |
| 删孤儿脚本(#7) | 删除 pms/deploy/init_db.py | 无引用、硬编码版本号不存在于迁移链,误执行会搞乱迁移 |
| 健康探针(#5) | health.py 加轻量探针:SELECT 1(MySQL)+ PING(Redis),任一失败返回 503;注意用短超时,避免 DB 慢时误摘除;补测试 | remote-deploy.sh 的健康检查自动受益 |
| 前端测试骨架(#9) | 引入 Vitest + @testing-library/react,先补 3 个核心用例:formatError、ProtectedRoute(redirect 回跳)、RequireRole(403 出口);接入 CI(若 CI 有前端 job) | 本轮刚改的核心逻辑最需要防回归 |

### 批次 2:需确认后执行

| 项 | 动作 | 需要确认什么 |
|---|---|---|
| 收端口(#6) | compose.prod 删除 mysql 3307、redis 6379 的 ports 映射 | **你或运维有没有用 3307/6379 从宿主机或其他机器直连过数据库/Redis?**(我排查时都用 docker exec,不依赖端口) |
| import_objectives.py(#8) | 移到 scripts/archive/ 加 README,或直接删除 | **这个 6 月的一次性导入脚本以后还会用吗?**现在 HrConsole 有正规导入入口 |

### 明确不修(记录在案)

- 同步 SQLAlchemy 阻塞(#10):pool_size=20 已够,200+ 用户再评估异步迁移
- 导出 OOM(#11):200 行限制下无实际风险,放开上限时改流式
- antd 1.17MB(#12):无可靠瘦身途径,维持

---

*批次 1 确认后可直接执行;批次 2 等待两个确认。*

---

## 三、执行结果(2026-08-05 当天)

**批次 1 已完成**(用户批准):

- AGENTS.md 纠偏 5 处(pymysql 同步栈、react-router-dom ^6.27、§4.1 改"同步风格"、§7.1 排查条目、vite ^5.4——执行中发现的第 4 处漂移,HARNESS.md 无需改)
- pyproject requires-python 收紧 >=3.12,<3.13
- 删除 init_db.py(孤儿危险脚本)和 import_objectives.py(用户授权我评估后决定删除,已有正规导入入口,git 历史可追溯)
- /health 加 MySQL SELECT 1 + Redis PING 探针,失败 503;新增 test_health.py 3 用例
- 前端 Vitest 骨架落地:7 用例全过(formatError、ProtectedRoute、RequireRole),已接入 CI 的 frontend-build job;新增 devDependencies:vitest@2(vite 5 兼容)、@testing-library/react/dom/jest-dom、jsdom

**验证**:pytest 197 全过、vitest 7 全过、tsc/build/ruff 通过。

**批次 2 状态**:端口回收用户要去问运维(待确认);import_objectives.py 已按授权删除(批次 2 剩余仅端口一项)。

**P2 三项维持不修**,理由见上文。
